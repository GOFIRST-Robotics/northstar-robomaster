// namespace control::algorithms
// {
    
// class SlewRateLimiter
// {
//     public:
//         SlewRateLimiter(float rateLimit, float maxError) : rateLimit(rateLimit / 60000), maxError(maxError) {}
//         float runLimiter(float desiredVelocity, float currentVelocity);
//     private:
//         float rateLimit; //Motor rotations per minute
//         float maxError;
//         uint32_t timeDiff{0};
//         uint32_t prevTime{0};
// };

// }
